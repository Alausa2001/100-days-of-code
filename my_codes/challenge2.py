#this program takes in the age of the user and add
#the first two digit
age = input("type in your age(in qoutes: for example '33' not 33\n")
first_digit = int(age[0])
second_digit= int(age[1])
print((first_digit) + (second_digit))
